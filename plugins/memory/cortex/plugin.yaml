name: cortex
version: 0.1.0
description:
  "Cortex memory — hand-curated markdown knowledge base with FTS5 search, auto-prefetch
  on each turn, and durable cross-session recall."
hooks:
  - prefetch
  - sync_turn
  - on_session_end
  - on_pre_compress
